function navbar(title) {
	console.log(title);
}

navbar('HTML');
navbar('CSS');
navbar('JS');
navbar('React');

const arr = [2, 3, 5];

arr.forEach((value, index) => {
	console.log(value, value);
});
